<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.georgekolobaev.rduhelper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Randomiser"></activity>
        <activity android:name=".HelpfulLinks" />
        <activity android:name=".Helpful_links" />
        <activity android:name=".YoutubeActivity" />
        <activity android:name=".NavigatorActivity" />
        <activity android:name=".Federations" />
        <activity android:name=".Inst" />
        <activity android:name=".Add" />
        <activity android:name=".Popovswords" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>